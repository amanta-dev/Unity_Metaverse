*

# 릴리스 서명 키 — 절대 커밋 금지 (분실/유출 시 앱 서명 영구 불가)
# 위 '*' 로 이미 무시되지만, 화이트리스트가 바뀌어도 키는 항상 제외되도록 명시.
*.keystore
*.jks

# Builds/WebGL 허용
# !Builds/
# !Builds/WebGL/
# !Builds/WebGL/**

# GameData 허용
!GameData/
!GameData/**

# docs 허용 (GitHub Pages — 개인정보처리방침/약관)
!docs/
!docs/**
